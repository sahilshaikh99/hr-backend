name: CICD

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - name: Checkout source
        uses: actions/checkout@v3
      - name: Login to docker hub
        run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }} 
      - name: Build docker image
        run: docker build -t hr-system/backend .
      - name: Publish image to docker hub
        run: docker push hr-system/backend:latest
        
  deploy:
    needs: build
    runs-on: [ip-172-31-36-135]
    steps:
      - name: Pull image from docker hub
        run: docker pull hr-system/backend:latest
      - name: Delete old container
        run: docker rm -f backend-container
      - name: Run docker container
        run: docker run -d -p 5000:5000 --name backend-container hr-system/backend
